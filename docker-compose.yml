version: '3.8'

services:
  ollama:
    image: ollama-deepseek
    container_name: ollama-deepseek
    restart: unless-stopped
    ports:
      - "11434:11434"
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: all
              capabilities: [gpu]
  